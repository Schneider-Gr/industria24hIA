# Segredos — nunca versionar
.env
.env.*
!.env.example

# Dados exportados com dados de usuários
export_*.csv
industria24h_reverse_export.zip

# Build / dependências
node_modules/
.next/
*.tsbuildinfo

# Graphify — cache e backups (graph.json/html ficam versionados)
graphify-out/cache/
graphify-out/20*/
